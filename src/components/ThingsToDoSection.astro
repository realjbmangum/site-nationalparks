---
/**
 * Things To Do Section Component
 *
 * Enhanced activities section with NPS data.
 * Shows activity cards with duration, difficulty, and categories.
 */
import type { NPSThingToDo } from '../lib/nps-api';

interface Props {
  thingsToDo: NPSThingToDo[];
  parkCode: string;
}

const { thingsToDo, parkCode } = Astro.props;

// Show first 6 activities
const displayActivities = thingsToDo.slice(0, 6);

// Get primary activity type
function getActivityType(thing: NPSThingToDo): string {
  if (thing.activities?.length > 0) {
    return thing.activities[0].name;
  }
  if (thing.topics?.length > 0) {
    return thing.topics[0].name;
  }
  return 'Activity';
}

// Format duration
function formatDuration(thing: NPSThingToDo): string | null {
  if (thing.durationDescription) {
    // Extract just the time part if it's verbose
    const match = thing.durationDescription.match(/(\d+[-–]\d+\s*(hours?|minutes?|hrs?|mins?))/i);
    if (match) return match[1];
    if (thing.durationDescription.length < 20) return thing.durationDescription;
  }
  if (thing.duration) {
    return thing.duration;
  }
  return null;
}

// Get season badges
function getSeasons(thing: NPSThingToDo): string[] {
  return (thing.season || []).slice(0, 2);
}
---

{displayActivities.length > 0 && (
  <section class="things-to-do-section">
    <button class="section-toggle" id="thingstodo-toggle" aria-expanded="false" aria-controls="thingstodo-content">
      <div class="toggle-left">
        <h2 class="section-title">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="title-icon">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6.75 3v2.25M17.25 3v2.25M3 18.75V7.5a2.25 2.25 0 012.25-2.25h13.5A2.25 2.25 0 0121 7.5v11.25m-18 0A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75m-18 0v-7.5A2.25 2.25 0 015.25 9h13.5A2.25 2.25 0 0121 11.25v7.5m-9-6h.008v.008H12v-.008zM12 15h.008v.008H12V15zm0 2.25h.008v.008H12v-.008zM9.75 15h.008v.008H9.75V15zm0 2.25h.008v.008H9.75v-.008zM7.5 15h.008v.008H7.5V15zm0 2.25h.008v.008H7.5v-.008zm6.75-4.5h.008v.008h-.008v-.008zm0 2.25h.008v.008h-.008V15zm0 2.25h.008v.008h-.008v-.008zm2.25-4.5h.008v.008H16.5v-.008zm0 2.25h.008v.008H16.5V15z"/>
          </svg>
          Things To Do
        </h2>
        <span class="item-count">{thingsToDo.length} activit{thingsToDo.length !== 1 ? 'ies' : 'y'}</span>
      </div>
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="chevron-icon">
        <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 8.25l-7.5 7.5-7.5-7.5"/>
      </svg>
    </button>

    <div class="section-content" id="thingstodo-content">
      <div class="activities-grid">
      {displayActivities.map((thing) => {
        const activityType = getActivityType(thing);
        const duration = formatDuration(thing);
        const seasons = getSeasons(thing);

        return (
          <a
            href={thing.url}
            target="_blank"
            rel="noopener noreferrer"
            class="activity-card"
          >
            {thing.images?.[0] && (
              <div class="activity-image">
                <img
                  src={thing.images[0].url}
                  alt={thing.images[0].altText || thing.title}
                  loading="lazy"
                />
              </div>
            )}

            <div class="activity-content">
              <span class="activity-type">{activityType}</span>

              <h3 class="activity-title">{thing.title}</h3>

              {thing.shortDescription && (
                <p class="activity-description">
                  {thing.shortDescription.length > 100
                    ? thing.shortDescription.slice(0, 100) + '...'
                    : thing.shortDescription}
                </p>
              )}

              <div class="activity-meta">
                {duration && (
                  <span class="meta-item">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="meta-icon">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M12 6v6h4.5m4.5 0a9 9 0 11-18 0 9 9 0 0118 0z"/>
                    </svg>
                    {duration}
                  </span>
                )}

                {seasons.length > 0 && (
                  <span class="meta-item">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="meta-icon">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M6.75 3v2.25M17.25 3v2.25M3 18.75V7.5a2.25 2.25 0 012.25-2.25h13.5A2.25 2.25 0 0121 7.5v11.25m-18 0A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75m-18 0v-7.5A2.25 2.25 0 015.25 9h13.5A2.25 2.25 0 0121 11.25v7.5"/>
                    </svg>
                    {seasons.join(', ')}
                  </span>
                )}

                {thing.isReservationRequired === "true" && (
                  <span class="reservation-badge">Reservation Required</span>
                )}
              </div>
            </div>
          </a>
        );
      })}
      </div>

      {thingsToDo.length > 6 && (
        <a
          href={`https://www.nps.gov/${parkCode}/planyourvisit/things2do.htm`}
          target="_blank"
          rel="noopener noreferrer"
          class="view-all-link"
        >
          View All {thingsToDo.length} Activities
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="arrow-icon">
            <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3"/>
          </svg>
        </a>
      )}
    </div>
  </section>
)}

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const toggle = document.getElementById('thingstodo-toggle');
    const content = document.getElementById('thingstodo-content');

    if (toggle && content) {
      toggle.addEventListener('click', () => {
        const isExpanded = toggle.getAttribute('aria-expanded') === 'true';
        toggle.setAttribute('aria-expanded', String(!isExpanded));
        content.classList.toggle('expanded');
      });
    }
  });
</script>

<style>
  .things-to-do-section {
    margin-top: 2rem;
    background: white;
    border-radius: 0.75rem;
    border: 1px solid var(--color-gray-200, #e5e7eb);
    overflow: hidden;
  }

  .section-toggle {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
    padding: 1rem 1.25rem;
    background: none;
    border: none;
    cursor: pointer;
    transition: background-color 0.15s;
  }

  .section-toggle:hover {
    background: var(--color-gray-50, #f9fafb);
  }

  .toggle-left {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .section-title {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-display, 'Playfair Display', serif);
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--color-gray-900, #111827);
    margin: 0;
  }

  .title-icon {
    width: 1.25rem;
    height: 1.25rem;
    color: var(--color-primary, #1E4D2B);
  }

  .item-count {
    font-size: 0.875rem;
    color: var(--color-gray-500, #6b7280);
  }

  .chevron-icon {
    width: 1.25rem;
    height: 1.25rem;
    color: var(--color-gray-400, #9ca3af);
    transition: transform 0.2s ease;
  }

  .section-toggle[aria-expanded="true"] .chevron-icon {
    transform: rotate(180deg);
  }

  .section-content {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease;
  }

  .section-content.expanded {
    max-height: 3000px;
  }

  .view-all-link {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.375rem;
    margin: 1rem 1.25rem 1.25rem;
    padding: 0.625rem 1rem;
    background: rgba(30, 77, 43, 0.05);
    color: var(--color-primary, #1E4D2B);
    border-radius: 0.375rem;
    font-size: 0.875rem;
    font-weight: 500;
    text-decoration: none;
    transition: background-color 0.15s;
  }

  .view-all-link:hover {
    background: rgba(30, 77, 43, 0.1);
  }

  .arrow-icon {
    width: 1rem;
    height: 1rem;
  }

  .activities-grid {
    padding: 0 1.25rem;
    display: grid;
    grid-template-columns: repeat(1, 1fr);
    gap: 1rem;
  }

  @media (min-width: 640px) {
    .activities-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .activities-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .activity-card {
    background: white;
    border-radius: 0.75rem;
    border: 1px solid var(--color-gray-200, #e5e7eb);
    overflow: hidden;
    text-decoration: none;
    transition: box-shadow 0.2s, transform 0.2s;
  }

  .activity-card:hover {
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
    transform: translateY(-2px);
  }

  .activity-image {
    height: 8rem;
    overflow: hidden;
  }

  .activity-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .activity-content {
    padding: 1rem;
  }

  .activity-type {
    display: inline-block;
    font-size: 0.6875rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.025em;
    color: var(--color-primary, #1E4D2B);
    background: rgba(30, 77, 43, 0.1);
    padding: 0.125rem 0.5rem;
    border-radius: 0.25rem;
    margin-bottom: 0.5rem;
  }

  .activity-title {
    font-weight: 600;
    font-size: 1rem;
    color: var(--color-gray-900, #111827);
    margin: 0 0 0.375rem 0;
    line-height: 1.3;
  }

  .activity-description {
    font-size: 0.8125rem;
    color: var(--color-gray-600, #4b5563);
    margin: 0 0 0.75rem 0;
    line-height: 1.4;
  }

  .activity-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    align-items: center;
  }

  .meta-item {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    font-size: 0.75rem;
    color: var(--color-gray-500, #6b7280);
  }

  .meta-icon {
    width: 0.875rem;
    height: 0.875rem;
  }

  .reservation-badge {
    font-size: 0.6875rem;
    font-weight: 500;
    color: var(--color-secondary, #F4A300);
    background: rgba(244, 163, 0, 0.1);
    padding: 0.125rem 0.5rem;
    border-radius: 0.25rem;
  }
</style>
